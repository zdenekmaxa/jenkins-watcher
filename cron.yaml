cron:
- description: Retrieve overview data from the Jenkins CI server
  url: /update_overview_check_builds
  schedule: every 15 minutes

- description: Retrieve builds information from the Jenkins CI server
  url: /update_builds_stats
  schedule: every 1 hours

- description: Send activity summary email
  url: /send_summary
  schedule: every 12 hours